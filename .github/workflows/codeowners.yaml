---
name: codeowners

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/codeowners.yaml
      - go.mod
      - go.sum
      - "**.go"
      - "**.ts"
      - "**.yaml"

jobs:
  deploy:
    runs-on: ubuntu-latest

    permissions:
      contents: write
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Install Go
        uses: actions/setup-go@v6
        with:
          go-version: stable

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10
          run_install: true

      - name: Install Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Build packages
        run: pnpm run --recursive --filter @domains/codeowners... build

      - name: Generate codeowners
        run: pnpm run generate
        working-directory: codeowners/

      - name: Update codeowners
        run: |
          git config --local user.email "actions@github.com"
          git config --local user.name "GitHub Actions"
          git add CODEOWNERS
          git commit -m ":wrench: Update codeowners" || exit 0
          git push
